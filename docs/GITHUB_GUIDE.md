# GitHubGuide
## コーディングの始め方

1. リポジトリをローカルに **Clone** します
	SSHでCloneしてほしいので以下のものをそのまま貼り付けてください。
```
git clone git@github.com:RyukokuDX/STEAM_tracker.git
```
    
2. プロジェクトのディレクトリに移動し、依存関係をインストールします:
```
cd YOUR_REPOSITORY
npm install
```
    
3. 変更内容に応じたブランチを作成します。ブランチ名は自分の名前を入れて分かりやすいものにしてください。
		必ずブランチ名は**苗字**を使ってください。また苗字の頭は**小文字**でお願いします。
```
# 例: ログイン機能を作成するとき
git checkout -b mitani/feature/login
```
4. 作業を開始してください
5. 切りが良いタイミングや一日の作業の終わりのタイミングで**Commit**してください
	そのときコミットメッセージを書いてもらうのですがそれは以下のフォーマットでお願いします。
```
git commit -m "コミットメッセージ"
```
その後
```
git push
```
これで基本の作業は終了です。
## コミットメッセージの規約
### フォーマット

```
<type>(<scope>): <subject>
```

### `<type>`: 変更の種類 (必須)

|                |                                  |
| -------------- | -------------------------------- |
| **タイプ**        | **説明**                           |
| **`add`**     | 新機能の追加                           |
| **`fix`**      | バグ修正                             |
| **`docs`**     | ドキュメントのみの変更                      |
| **`style`**    | コードの動作に影響しないスタイルの変更（フォーマット、空白など） |
| **`refactor`** | バグ修正や機能追加ではないコードの改善（リファクタリング）    |
| **`perf`**     | パフォーマンスを向上させる変更                  |
| **`test`**     | テストコードの追加・修正                     |
| **`chore`**    | ビルドプロセスや補助ツールなど、ソースコード以外の雑多な変更   |

### `<scope>`: 変更の範囲 (任意)

変更が影響する範囲を `()` で示します。（例: `auth`, `api`, `ui`）
わからなかったら大丈夫です。

### `<subject>`: 変更内容の要約 (必須)

- 50文字以内の簡潔な説明を **命令形** で記述します。（例: `○○○○を追加`, `□□□□を修正`）
- 文末に句点（。）は付けません。
### 具体例

**良い例 👍**

```
feat(auth): ユーザー登録機能を追加
```

```
fix(api): 無効なIDが渡された際のnullエラーを修正
```

**悪い例 👎**

```
fix
```

> → typeがなく、内容が不明


## プルリクエストの作成

ブランチでの作業が完了し、developブランチにマージしてほしい場合は、プルリクエストを作成してください。

1.  GitHubのリポジトリページにアクセスし、「Pull requests」タブから「New pull request」をクリックします。
2.  `base` ブランチが `develop`、`compare` ブランチが自分の作業ブランチになっていることを確認します。
3.  変更内容を確認し、「Create pull request」をクリックします。
4.  テンプレートに従って、プルリクエストのタイトルと詳細を記述します。
    -   **概要**: 変更の目的を簡潔に書きます。
    -   **変更点**: 具体的な変更内容をリストで書きます。
    -   **関連イシュー**: 関連するIssueがあればリンクします。
    -   **確認事項**: チェックリストを埋めて、セルフレビューを実施します。
5.  「Create pull request」をクリックして、作成を完了します。

<!-- ## コーディング規約

コードの品質と一貫性を保つため、以下の規約を遵守してください。
### 命名規則

- **変数・関数**: `camelCase` (例: `userName`, `getUserData`)
    
- **クラス・コンポーネント**: `PascalCase` (例: `UserAuth`, `LoginComponent`)
    
- **定数**: `UPPER_SNAKE_CASE` (例: `MAX_LOGIN_ATTEMPTS`)
### コメント

- **「なぜ」を記述する**: 複雑なロジックや、一見して意図が分かりにくい箇所には、「何をしているか」ではなく **「なぜその実装にしたのか」** を説明するコメントを残してください。
- **自明なコメントは不要**: コードを読めば分かるようなコメントは書かないでください。 -->
